import 'package:flutter/material.dart';

class AllPadings {
  final generalPadding = const EdgeInsets.symmetric(horizontal: 30, vertical: 20);
  final firstTitlePadding = const EdgeInsets.only(top: 80, bottom: 60);
  final firstTextFieldPadding = const EdgeInsets.only(bottom: 10);
}
